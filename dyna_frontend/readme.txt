this directory should eventually contain the parser and the repl

this can then call into other modules which implement the backend operations


The parser should be eventually moved into this package.  There should be hooks
into the different back end representations to allow them to run
