import importlib

# TODO: make it so that .dyna files can be imported into python programs.  Then
# it an make it so that there is some wrapper of the runtime where different
# methods can be invoked directly as a reult.
#
# This should make it easy to proxy between the dyna core internals python as a
# host language.
