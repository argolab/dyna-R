(ns dyna-backend.core-test
  (:require [clojure.test :refer :all]
            [dyna-backend-java2.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))


(deftest basic-rexpr
  (let [expr '(if expression )])
  )